# Credenciales del usuario de prueba para e2e tests (Playwright).
# Copia este archivo como .env.e2e y completa los valores reales.
# NUNCA commitees .env.e2e — contiene secrets reales.

E2E_USER_EMAIL=user@example.com
E2E_USER_PASSWORD=change-me
